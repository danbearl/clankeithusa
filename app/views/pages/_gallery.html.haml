.span9
  %p 
    :markdown
      #{page.body}
  - page.images.each do |image|
    .image.float-left
      = link_to image_tag(image.picture_url(:thumb), class: 'img-polaroid'), image.picture_url
      %br
      = image.name

- model_class = Page

.page-header
  %h1=t '.title', default: t('helpers.titles.new', model: model_class.model_name.human, default: "New Event")

= form_for event, builder: Formal::Builder do |f|
  = f.error_messages
  = render partial: "form", locals: { f: f}

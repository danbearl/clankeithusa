- model_class = Event
.page-header
  %h1=t '.title', default: t('helpers.titles.edit', model: model_class.model_name.human, default: "Edit Event")

= form_for event, url: event_path(event) do |f|
  = render partial: "form", locals: { f: f }
